# DRS Pro branding assets

| File | Purpose |
|------|---------|
| `icon.ico` | Windows app + installer icon (PyInstaller, Inno Setup) |
| `icon.png` | Source PNG for the icon |

**Design:** Top-down LBW corridor — green pitch strip, two yellow stump-corridor lines, red ball on the wicket line, white circular badge for dark title bars.

Regenerate:

```powershell
python tools/generate_icon.py
```
